[project]
name = "voice-agent"
version = "0.1.0"
description = "Deepgram-powered voice agent with Twilio integration for pharmacy operations"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.1",
    "websockets>=15.0.1",
]

[project.scripts]
voice-agent = "app.server:run"
